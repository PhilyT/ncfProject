<% var title = 'Badge etudiant' %>
<% include head %>
<div class="container" >
    <div class="row centered-form">
            <div class="col-xs-12 col-sm-12 col-md-6 col-sm-offset-6 col-md-offset-3">
                <div class="panel panel-default">
                    <div class="panel-heading">
                        <h1 class="panel-title"><strong>Badgeuse Etudiante  </strong></h1>
                    </div>
                    <div class="panel-body">
                   
						<div class="alert alert-info" role="alert">  <h1 class="text-center"> Scanner votre carte </h1> </div>
                        <form method="post" action="/badgeetudiant">

                            <input type="submit" value="lecture" class="btn btn-info btn-block"/>
                        </form>
                         <% if(etat != ""){ %>
                            <% if(etat == "success") {%>
                            <div class="alert alert-success" role="alert"> <h1>Etudiant : <%= prenom %> <%= nom %></h1> </div>
                            <% } else {%>
                                <p><%= etat %></p>
                        <% }} %>
                    	<a href="/login"><span class="glyphicon glyphicon-arrow-left"></span> Retour</a>
                    </div>
                </div>
            </div>
        </div>
    </div>

<% include foot%>